/**
 * Reserved for future Kairos-specific spatial queries (e.g. line-of-sight
 * helpers for the Toxotes ranged attack visualization).
 *
 * Current logic lives in `src/game/engine/moves.ts` (rules) and
 * `src/grid/Grid.ts` (world conversion).
 */
export {};

