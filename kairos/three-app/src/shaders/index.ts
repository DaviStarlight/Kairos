/**
 * Reserved: custom ShaderMaterial registry for advanced FX (rim glow, tile pulse,
 * particle billboards). Today the project relies on MeshStandardMaterial + emissive
 * channel animations driven by GSAP for performance and visual consistency.
 *
 * Add new shaders here and export them so other systems can import & swap materials
 * without touching the rest of the codebase.
 */
export {};
