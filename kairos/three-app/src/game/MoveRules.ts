/**
 * Deprecated — Kairos rules now live in `./engine/moves.ts`.
 * Re-exported here for any external import-by-path consumers.
 */
export {
  getMovesForPiece,
  hasAnyLegalMove,
  canPlayerPress,
} from './engine/moves';

